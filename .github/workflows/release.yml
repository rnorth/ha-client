name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        # actions/checkout@v4
        with:
          fetch-depth: 0
          # fetch-depth: 0 required — goreleaser uses full git history for
          # version detection and changelog generation

      - uses: jdx/mise-action@c37c93293d6b742fc901e1406b8f764f6fb19dac
        # jdx/mise-action@v2

      - name: Run GoReleaser
        run: goreleaser release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
